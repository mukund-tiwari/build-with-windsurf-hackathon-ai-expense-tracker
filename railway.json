{
  "services": [
    {
      "name": "backend",
      "path": "app",
      "start": "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
    },
    {
      "name": "frontend",
      "path": "frontend",
      "start": "npm install && npm run build && npm run start -- --port $PORT"
    }
  ]
}